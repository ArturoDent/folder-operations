{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "echoMe",
      "type": "shell",
      "command": "${command:workbench.action.terminal.newWithCwd} ${input:cwd}",
      "args": [
      
      ]
    }
  ],
  "inputs": [
    {
      "id": "cwd",
      "type": "command",
      "command": "echo extension.randomGuid"
    },
  ]
}