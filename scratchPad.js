				// const textEdits = [];
				// textEdits.push( vscode.TextEdit.insert(new vscode.Position(line, endOfLine), `${spaceString}${startChar}`));

const { IndexKind } = require("typescript");

				// await editor.edit(builder => {
				// 	builder.insert(new vscode.Position(line, endOfLine), `${spaceString}${startChar}`)
				// });

				// const workEdits = new vscode.WorkspaceEdit();
				// workEdits.set(document.uri, textEdits); // give the edits
				// await vscode.workspace.applyEdit(workEdits); // apply the edits

				code --goto file://test/suite/index.js:42:4

				https://github.com/eamodio/vscode-gitlens
